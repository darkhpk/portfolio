# Django Settings
SECRET_KEY=tureco-assignment182Av2455aTgsaA23
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,87.106.96.193

# Admin URL (Change for security - e.g., secret-admin-panel/)
ADMIN_URL=admin/

# Database (optional - defaults to SQLite)
# Uncomment and configure for PostgreSQL on production
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=tureco_db
# DATABASE_USER=tureco_user
# DATABASE_PASSWORD=your_db_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Email Configuration (for production)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@tureco.com
# SERVER_EMAIL=server@tureco.com

# Redis (for caching and channels in production)
REDIS_URL=redis://127.0.0.1:6379/1

# Static and Media Files
# These are configured in settings.py but can be overridden if needed
# STATIC_URL=/static/
# MEDIA_URL=/media/
